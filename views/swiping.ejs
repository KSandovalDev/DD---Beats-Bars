<!doctype html>
<html>
<head>
    <title>Beats & Bars</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="assets/css/profile.css">
    <link rel="stylesheet" href="/assets/css/default.css">
    <link rel="stylesheet" href="/assets/css/main.css">
    <link rel="stylesheet" href="/assets/css/theme-orange.css">
    <script type="text/javascript" src="assets/js/swiping.js"></script>
</head>
<body>
    <div class="clearfix booking-form neon-lit-background">
        <div class="booking-form__heading no-print">
             <div>
                <div class="neon-border neon-border--flush neon-border--blue neon-border--topgap-xxs neon-border--bottomgap-xxs">
                    <h1 class="h1 type-decorative text-glow pink">BEATS     <span class="h1 annoying type-decorative text-glow pink">   &   </span>   BARS</h1>

                    
                </div>
            </div>
        </div>
    </div>

    <div id="navBtns">
        <a href="/home" class=" navLink btn btn-default btn-sm">Profile</a>
        <a href="/swiping" class="navLink btn btn-default btn-sm">Swipe</a>
        <a href="/matches" class=" navLink btn btn-default btn-sm">Matches</a>
        <a href="/logout" class=" navLink btn btn-default btn-sm">Logout</a>
    </div>


    <div class="swipeBtns">
        <button class="swipeLeft" id="skip">Skip</button>
        <button class="swipeRight" id="play">Play</button>
    </div>

    <div class="container">
    <!-- if you want to display a string in EJS use = if you wanna display code use - -->
            <div class="cardsContainer">
                <% for(let i =0;i<result.length;i++){ %>
                    <div class="stageProfile">
                        <img src="<%= result[i].imgPath %>" alt="" class="avatar"> 
                        <form action="/likeUser" method="POST" >
                            <input type="text" name="userEmail" value="<%= result[i].username %>" hidden>
                            <button class="play" type="submit">Play</button>
                        </form>
                        <!-- <h3>First Name: <%= result[i].firstname %></h3>
                        <h3>Last Name: <%= result[i].lastname %></h3> -->
                        <h3><%= result[i].name %>, <%= result[i].age %></h3>
                        <h3>Bio: <%= result[i].bio %></h3>
                        <h3>Genres: <%= result[i].genre1 %>, <%= result[i].genre2 %>, <%= result[i].genre3 %>, <%= result[i].genre4 %></h3>
                        <!-- <h3>Influences: <%= result[i].influences %></h3> -->
                        <h3>Instruments & Level <%= result[i].instrument %>, Level: <%= result[i].instrumentLevel %></h3>
                        <!-- <h3>Instagram: <%= result[i].instagramLink %></h3> -->
                        <h3>Looking for: <%= result[i].looking %></p>
                        <p><%- result[i].spotifyCode %></p>
                        <p><%- result[i].youtubeCode %></p>
                        <p><%- result[i].soundCloudCode %></p>
                    </div>
                <% } %>
            </div>
    </div>
    
   

    

    <script>
        var cont = $(".neon-lit-background");
var neon = $(".neon-border");
setInterval(function() {
  var i = Math.floor((Math.random() * 10) + 1) * 200;
  setTimeout(function() {
    setTimeout(function() {
        $(cont).addClass("neon-lit-background--poweroff");
        $(neon).addClass("neon-border--poweroff");
      }, i),
      $(cont).removeClass("neon-lit-background--poweroff");
    $(neon).removeClass("neon-border--poweroff");
  }, 1000);
}, 2000);
    </script>
</body>
</html>
